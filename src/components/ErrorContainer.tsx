export default async function ErrorNotFound() {
    return (
        <main className="
            absolute bg-[rgb(255,0,0)]/50 w-full h-full
            top-0 left-0 pointer-events-none
        ">

        </main>
    );
}